# 本仓库的 npm/registry 约定。
#
# 背景：开发机全局 registry 通常指向 npmmirror（安装快），但**发布**必须走官方 registry。
# 这里只把 @excellence-wh 这个 scope 指向官方源，其余依赖安装仍然走本机配置的镜像。
@excellence-wh:registry=https://registry.npmjs.org/

# 不要把 token 写进本文件；用 `npm login`（写入用户级 ~/.npmrc）
# 或在 CI 中通过 NPM_TOKEN 环境变量提供。
